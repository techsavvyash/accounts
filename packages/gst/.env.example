# HSN API Configuration
# This file documents the environment variables you can set to enable HSN API providers

# =============================================================================
# E-way Bill Government API Configuration
# =============================================================================
# To use the official Government of India E-way Bill API, you need to:
# 1. Register on the E-way Bill portal (https://ewaybillgst.gov.in/)
# 2. Enable API access in your account
# 3. Generate API credentials
#
# Docs: https://docs.ewaybillgst.gov.in/apidocs/

# Enable E-way Bill provider (set to 'true' to enable)
EWAYBILL_ENABLED=false

# E-way Bill API base URL
EWAYBILL_BASE_URL=https://api.ewaybillgst.gov.in

# Your E-way Bill username
EWAYBILL_USERNAME=

# Your E-way Bill password
EWAYBILL_PASSWORD=

# Your E-way Bill application key
EWAYBILL_APP_KEY=

# Your GSTIN
EWAYBILL_GSTIN=

# Optional: Client ID (for GSP integration)
EWAYBILL_CLIENT_ID=

# Optional: Client Secret (for GSP integration)
EWAYBILL_CLIENT_SECRET=

# Priority (lower number = higher priority, 1 is highest)
EWAYBILL_PRIORITY=1

# Request timeout in milliseconds
EWAYBILL_TIMEOUT=5000

# Enable caching (set to 'false' to disable)
EWAYBILL_CACHE_ENABLED=true

# Cache TTL in milliseconds (default: 24 hours)
EWAYBILL_CACHE_TTL=86400000

# =============================================================================
# Sandbox.co.in API Configuration
# =============================================================================
# Sandbox.co.in is a third-party Tax API provider for India
# - 14-day free trial available
# - Paid subscription required after trial
#
# Docs: https://developer.sandbox.co.in/reference/get-hsn-details-api
# Pricing: https://sandbox.co.in/pricing

# Enable Sandbox provider (set to 'true' to enable)
SANDBOX_ENABLED=false

# Sandbox API base URL
SANDBOX_BASE_URL=https://api.sandbox.co.in/v2

# Your Sandbox API key
SANDBOX_API_KEY=

# Your Sandbox API secret
SANDBOX_API_SECRET=

# Priority (lower number = higher priority, 1 is highest)
SANDBOX_PRIORITY=2

# Request timeout in milliseconds
SANDBOX_TIMEOUT=5000

# Enable caching (set to 'false' to disable)
SANDBOX_CACHE_ENABLED=true

# Cache TTL in milliseconds (default: 24 hours)
SANDBOX_CACHE_TTL=86400000

# =============================================================================
# Usage Notes
# =============================================================================
#
# 1. The system works out-of-the-box with hard-coded HSN data
#    - No API configuration required for basic usage
#    - Includes 99 HSN chapters and 100+ common codes
#
# 2. Enable API providers to get:
#    - More comprehensive HSN code coverage
#    - Latest GST rate updates
#    - Official government data (E-way Bill)
#
# 3. Multiple providers:
#    - The system tries providers in priority order
#    - Falls back to hard-coded data if all APIs fail
#    - Built-in caching reduces API calls
#
# 4. Provider priority:
#    - Lower number = higher priority
#    - E-way Bill (priority 1) is tried before Sandbox (priority 2)
#    - Adjust priorities based on your preference
#
# 5. Caching:
#    - Enabled by default (24 hours)
#    - Reduces API costs and improves performance
#    - Adjust CACHE_TTL based on your needs
#
# =============================================================================
